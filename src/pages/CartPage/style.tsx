import styled from "styled-components";

const StyledButton = styled.button`
  color: #ffff;
  font-size: 30px;
  
  img {
    width: 250px;
    object-fit: cover;
  }
`;

export default StyledButton;
